currencies = {"dollar": "$",
              "rupees": u"\u20B9",
              "yen": 	u"\u00A5",
              # =============================================================================
              #              ''' other currencies can be added,
              #               by using appropriate unicode of that currency'''
              #
              # =============================================================================
              }

tuple_ = {'single': '1',
          'double': '2',
          'triple': '3',
          'quadruple': '4',
          # =============================================================================
          # '''these are the most commonly used tuples,
          # other tuples can be added as per the needs'''
          # =============================================================================

          }

#print(type(set(tuple_.keys())))
# currency_symbol = currencies['yen']
# print(type(currency_symbol))
